# Backend environment variables - copy to .env, DO NOT commit .env
# Generate a Fernet key:
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=__generate_with_command_above__

PORT=8000
ALLOWED_ORIGINS=http://localhost:5173
DATABASE_URL=sqlite:///./finimatic.db
FINIMATIC_ENVIRONMENT=development
FINIMATIC_AUTH_ENABLED=0

# All other credentials are stored through the Settings UI.
